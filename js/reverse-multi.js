//This is a funtion to reverse a sentenceusing leveraged code
function reverseString = funtion(text) {
  for (var i = text.length - 1, o = ' '; i >= 0; o += text[i--]) { }
  return o;
}
return result;
}
//return text.split

$(document).ready(function){
    $('.execute-reverse-multi').click(function() {
        var text = $('$reverse-input').val();
        $('reverse-output').val(reverseString(text));
        return false;
        )};
      $('.reset-reverse-multi').click(function() { 
       var text = $('$reverse-input','reverse-output').val();
      
        )}

